from odoo import models, fields,_

class interfax_config_settings(models.Model):
    _name='interfax.config.settings'

    username=fields.Char('Username')
    password=fields.Char('Password')
    active=fields.Boolean('Active')
